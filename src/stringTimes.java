public class stringTimes public String stringTimes(String str, int n) {
    String looped = "";
    for (int i=0; i<n; i++) {
        looped += str;
    }
    return looped;
}

//just make a loop that adds the string as many times as n
//ok first attempt not working
//nevermind i was using commas instead of semicolons to set up i{
}
